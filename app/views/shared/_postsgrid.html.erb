<div class="container padded">
	<div>
		<div class="home-grid col lg-24">
			<div class="home-posts">
				<div class="grid">
					<% posts.each do |post| %>
						<%= render partial: 'shared/postcard', locals: {post: post} %>
					<% end %>
				</div>
			</div>
		</div>
	</div>
</div>
